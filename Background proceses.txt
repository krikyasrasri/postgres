/usr/pgsql-13/bin/postgres -D /u01/pgsql/13

postgres: stats collector      - writes information to pg_stat* tables ; 24*7
postgres: autovacuum launcher  - remove dead tuples from tables. 

#autovacuum_work_mem = -1               
#autovacuum = on                        
#log_autovacuum_min_duration = 1s    
#autovacuum_max_workers = 3             
#autovacuum_naptime = 1min    
         
#autovacuum_vacuum_threshold = 50       
#autovacuum_vacuum_scale_factor = 0.2  

#autovacuum_analyze_threshold = 50     
#autovacuum_analyze_scale_factor = 0.1  

#autovacuum_vacuum_insert_scale_factor = 0.2 
#autovacuum_vacuum_insert_threshold = 1000   

#autovacuum_freeze_max_age = 200000000  

autovacuum_vacuum_threshold+autovacuum_vacuum_scale_factor*no.of records  

1000		0+0.001*1000 => 1

-> clear dead tuples (vacuum emp;)
-> gather statistics (analyze emp;)
-> Freeze old transaction IDs (vacuum freeze emp;)
-> release the space to OS (vacuum full emp;) CTAS

postgres: logger -> writes information to alert log

# REPORTING AND LOGGING

# - Where to Log - Where do you want me to log information?
logging_collector = on 
log_directory = 'log'
log_filename = 'postgresql-%a.log'
(or)
log_filename = 'postgresql-%d-%m-%Y.log'
log_destination = 'stderr' 

# - When to Log -  How much information you want?
#log_min_messages = warning 

# - What to Log -  What kind of information you want?

#log_checkpoints = off
#log_connections = off
#log_disconnections = off
#log_duration = off

postgres: walwriter  - writes information to wal file upon commit.
					 

postgres: checkpointer  - writes information to datafile.
						- updates controlfile about latest txid.

#checkpoint_timeout = 5min           
#checkpoint_completion_target = 0.9  
#checkpoint_flush_after = 256kB      
#checkpoint_warning = 30s            
#log_checkpoints = off

postgres: background writer - writes information to datafile.

#bgwriter_delay = 200ms           
#bgwriter_lru_maxpages = 100      
#bgwriter_lru_multiplier = 2.0    
#bgwriter_flush_after = 512kB     

local memory
-------------
work memory 			- sorting
maintenance work memory - vacuum
temp buffers 			- for temporary tables











































